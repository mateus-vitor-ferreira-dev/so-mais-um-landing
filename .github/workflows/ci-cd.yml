name: CI/CD - Só+1 Landing

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  ci:
    name: Lint & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm run test:ci

      # Os números do README saem do relatório que o passo acima grava. O
      # `test:ci` existe só por isso: `npm test` não emite JSON, e sem ele a
      # conferência não teria de onde ler.
      - name: Confere os números do README
        run: npm run readme:check

      # Fala com a api de produção. Sai com 2 quando ela não responde, e não
      # com 1: "api fora do ar" não é "a landing está errada", e o log precisa
      # dizer qual dos dois é — foi a mesma distinção que a collection teve que
      # fazer no `verifica-cobertura.py`.
      #
      # Cobre só o enumerável — modalidades, tags e os itens do roadmap.
      # Promessa em prosa é o item do `pull_request_template.md`.
      #
      # O roadmap é conferido contra o board: item cuja issue já fechou é
      # funcionalidade no ar, e a #62 achou os cinco de uma vez. Essa parte
      # precisa de `BOARD_TOKEN` — um PAT com leitura do repo da api, que é
      # privado; o `GITHUB_TOKEN` que o Actions injeta sozinho só enxerga este
      # repositório. Sem o segredo o passo NÃO fica vermelho: ele anuncia, no
      # log, que o roadmap não foi conferido.
      - name: Confere o contrato com o produto
        run: npm run contrato:check
        env:
          GITHUB_TOKEN: ${{ secrets.BOARD_TOKEN }}

      # Aviso, nunca reprovação.
      #
      # Vulnerabilidade nova aparece em dependência transitiva sem ninguém ter
      # tocado no repositório, e um PR reprovado por isso — numa sexta, no meio
      # de outra coisa — ensina a desligar o passo. O que se quer aqui é que ela
      # **apareça**; o que fazer com ela é decisão do board, não do CI.
      #
      # Mesma configuração na api e na web.
      - name: Confere as dependências
        run: |
          if ! npm audit --audit-level=high; then
            echo "::warning::O npm audit achou vulnerabilidade alta ou crítica. Rode 'npm audit' e leve para a issue de dependências."
          fi

      - name: Build
        run: npm run build

  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    needs: ci
    if: github.event_name == 'push'

    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Pull Vercel environment
        run: |
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            npx vercel pull --yes --environment=production --token=$VERCEL_TOKEN
          else
            npx vercel pull --yes --environment=preview --token=$VERCEL_TOKEN
          fi

      - name: Build project artifacts
        run: |
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            npx vercel build --prod --token=$VERCEL_TOKEN
          else
            npx vercel build --token=$VERCEL_TOKEN
          fi

      - name: Deploy
        run: |
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            npx vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN
          else
            npx vercel deploy --prebuilt --token=$VERCEL_TOKEN
          fi
